const express = require('express');
const router = express.Router();

const pujaController = require('../../controllers/puja');

//Obtener todos los productos por los que un usuario está pujando

module.exports = router;